// 文件名: package.json
{
  "name": "solaramy-worker-site",
  "version": "1.0.0",
  "description": "Cloudflare Worker Site deployment for Solaramy.",
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    // 您的 functions 目录是 TypeScript，因此需要 TypeScript 和类型定义
    "typescript": "^5.0.0",
    "@cloudflare/workers-types": "^4.20240403.0" 
  },
  "devDependencies": {
    // 部署和开发工具
    "wrangler": "^3.0.0" 
  }
}
